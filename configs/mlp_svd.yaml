model:
  name: "cifarmlp"
  
dataset:
  name: "cifar10"
  path: "data/cifar10"
  train_args:
    batch_size: 1024
  test_args:
    batch_size: 1024

training:
  warmup: false
  epochs: 50
  batch_size: 1024
  
  loss:
    type: "cross_entropy"
    label_smoothing: 0.0
  
  optimizer_groups:
    - name: "fc1_and_fc4_params"
      param_selector:
        type: "name_contains_multiple"
        patterns: ["fc1", "fc4"]
      optimizer:
        type: "adamw"
        lr: 0.0001
        weight_decay: 0
      lr_schedule:
        type: "linear_decay"
        total_steps: "total_epochs"

    - name: "hidden_params"
      param_selector:
        type: "name_contains_multiple"
        patterns: ["fc2", "fc3"]
      optimizer:
        type: "svd"
        lr: 0.01
        momentum: 0.95
        weight_decay: 0.001
        track_singulars: false
      lr_schedule:
        type: "linear_decay"
        total_steps: "total_epochs"

  wandb:
    project: "svd-mlp-sweep"
